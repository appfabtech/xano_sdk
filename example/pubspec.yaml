name: xano_sdk_example
description: >
  Example Flutter app demonstrating the xano_sdk realtime WebSocket channels.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  xano_sdk:
    path: ../          # local reference to the package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
